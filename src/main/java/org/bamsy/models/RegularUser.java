package org.bamsy.models;

public class RegularUser extends User {
    public RegularUser(String userId, String userName, String password, String role) {
        super(userId, userName, password, role);
    }
}
